import {StyleSheet, Text, Touchable, View} from 'react-native';

export const styles = StyleSheet.create({
  lower: {
    flex: 0,
    justifyContent: 'flex-end',
    paddingBottom: 20,
    zIndex: 9999,
    // position: 'relative',
  },
  cancel:{justifyContent: 'flex-end'},
  btnwrap: {
    flexDirection: 'row',
    justifyContent: 'space-around',
    //flex: 1
  },
  btnItem: {
   // flex: 1
  }
});
